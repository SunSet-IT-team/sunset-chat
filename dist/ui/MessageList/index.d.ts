import { Chat } from '../../model/types';
type MessageListProps = {
    chat: Chat;
};
/**
 * Вывод списка сообщений
 */
declare const MessageList: ({ chat }: MessageListProps) => import("react/jsx-runtime").JSX.Element;
export default MessageList;
